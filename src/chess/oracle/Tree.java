package chess.oracle;

public class Tree {
  private TreeNode root;

    public Tree() {
    }

    public Tree(Object rootData) {
        root = new TreeNode();
        root.setData(rootData);
    }
    
    

}
